<h1>Register</h1>

<% form_for @user, :html => { :class => 'hline_form' } do |f| %>
  <%= render :partial => "form", :object => f %>
  <div class="actions">
    <div class="submit"><%= f.submit "Register" %></div>
  </div>
<% end %>