<%= form_for(@queue_link) do |f| %>
  <% if @queue_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@queue_link.errors.count, "error") %> prohibited this queue_link from being saved:</h2>

      <ul>
      <% @queue_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="label"><%= f.label :artist_url %></div>
    <div class="input"><%= f.text_field :artist_url %></div>
  </div>
  <div class="field">
    <div class="label"><%= f.label :artist_name %></div>
    <div class="input"><%= f.text_field :artist_name %></div>
  </div>
  <div class="field">
    <div class="label"><%= f.label :imported %></div>
    <div class="input"><%= f.check_box :imported %></div>
  </div>
  <div class="actions">
    <div class="submit"><%= f.submit %></div>
  </div>
<% end %>